"""Django admin configuration for Cell Teacher application.

Currently no models are registered.
Add custom model registrations here when database models are created.
"""
from django.contrib import admin


# TODO: Register models when custom models are created

